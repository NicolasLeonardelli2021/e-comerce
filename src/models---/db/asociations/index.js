/* const User = require('../models/usuarios');
const Rol = require('../models/roles');


User.belongsTo(Rol,{
    foreignKey: 'rol_id'
});
Rol.hasOne(User,{
    foreignKey: 'rol_id'
})

Productos.belongsTo(Categoria,{
    foreignKey: 
})
 */